# 2026-05-11 工作记录

## 09:26 BTC日报自动执行
- 脚本正常运行，BTC=$81,724，策略=SHORT(置信度20)
- 报告生成、git push、Telegram推送均成功

## 15:55 修复策略入场区间计算逻辑（v2.3）
- **问题**：做空时 `entry_zone_high = min(price×1.008, ema20)`，EMA20远低于当前价（73621 vs 81724），导致入场上限被拉到73621，距当前价近8000点，几乎永远无法触发
- **修复**：入场区间改为锚定当前价 ±0.2%~0.5%，不再依赖EMA20/布林带截断
  - SHORT：`entry_low=price×0.998`, `entry_high=price×1.005`，止损=近期高点×1.005
  - LONG：`entry_low=price×0.995`, `entry_high=price×1.002`，止损=近期低点×0.995
  - 止盈：基于入场中点，按 2:1(TP1) / 3:1(TP2) 盈亏比计算
- **验证结果**：SHORT策略进场区间 $81,561~$82,133（宽度$572，贴近当前价），TP1=$79,717，TP2=$78,652
- **文件**：`C:/Users/asus/mk-trading/btc/run_daily_report.py`（`generate_strategy` 函数，第298行起）
- MEMORY.md 版本历史已更新至 v2.3
